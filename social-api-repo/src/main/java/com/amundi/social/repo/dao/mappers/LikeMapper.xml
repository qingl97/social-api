<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.amundi.social.repo.dao.mappers.LikeMapper">
	<resultMap type="com.amundi.social.common.model.Like" id="result">
		<result property = "appId" column = "app_id" />
		<result property = "productId" column = "product_id" />
		<result property = "userId" column = "user_id" />
		<result property = "timestamp" column = "timestamp" javaType="org.joda.time.DateTime" typeHandler="com.amundi.social.repo.dao.util.JodaDateTimeTypeHandler"/>
	</resultMap>
	
	<sql id="allLikes">
		select * from actions where type = 1
	</sql>

	<!-- Selects -->
	<select id="fetchAll" resultMap="result">
		<include refid="allLikes" />
	</select>

	<select id="fetchByApplication" resultMap="result">
		<include refid="allLikes" /> and app_id = #{appId};
	</select>
	
	<select id="fetchByProduct" resultMap="result">
		<include refid="allLikes" /> and app_id = #{appId} and product_id = #{productId};
	</select>
	
	<select id="fetchByUser" resultMap="result">
		<include refid="allLikes" /> and user_id = #{userId};
	</select>
	
	<select id="fetchByUserApplication" resultMap="result"> 
		<include refid="allLikes" /> and user_id = #{userId} and app_id = #{appId};
	</select>
	
	<!-- Insert -->
	<insert id="add" parameterType="string" useGeneratedKeys="true" keyProperty="id">
		insert into actions (type, app_id, product_id, user_id) values (1, #{appId}, #{productId}, #{userId});
		
		<selectKey keyProperty="id" resultType="int" order="AFTER">
			select last_insert_id() as id
		</selectKey>
	</insert>
	
	<!-- Delete -->
	<delete id="delete">
		delete from actions where type = 1 and app_id = #{appId} and product_id = #{product_id} and user_id = #{userId};
	</delete>
</mapper>